# Interaction Window Contract

Required fields: schema version, stable window ID, start/end, trigger event/kind, before/after page facts, typed reference arrays, and `association_basis`. Windows express `occurred_in_window`. They do not express cause, dependency, parameter provision, root cause, business result, or endpoint importance. Ordering and IDs are deterministic for frozen input.
